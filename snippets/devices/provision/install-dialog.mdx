A dialog will appear prompting you to install the `miru-agent`. If the device already has the correct `miru-agent` package installed, you can skip this step. 

[INSTALLATION DIALOG]

<Note>
  For more information about installing the `miru-agent` package, visit the [agent installation](/docs/developers/agent/install) documentation.
</Note>