import { ADMIN_TOOLTIP } from '/snippets/components/users/roles.jsx';

To log in, run the `login` command.

```bash
miru login
```

Retrieve your authentication token from the [Secrets](https://app.mirurobotics.com/settings/cli-token) page.

<Warning>
  You must be an <Tooltip tip={ADMIN_TOOLTIP.tip} cta={ADMIN_TOOLTIP.cta} href={ADMIN_TOOLTIP.href}>admin</Tooltip> user to retrieve an authentication token.
</Warning>

<Frame>
  <img
    src="https://assets.mirurobotics.com/docs/v03/images/secrets/cli-token/page.png"
    style={{ borderRadius:"16px" }}
  />
</Frame>

Paste the token into the CLI.

```
Please retrieve your authentication token from the following URL:
🔗 https://app.mirurobotics.com/settings/cli-token

🔑 Paste your authentication token: **********

Validating authentication token...
✅ Successfully logged in as Benjamin
```
