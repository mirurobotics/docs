# Add the CLI v0.12.1 changelog entry

This ExecPlan is a living document. The sections Progress, Surprises & Discoveries, Decision Log, and Outcomes & Retrospective must be kept up to date as work proceeds.


## Scope

| Repository | Access | Description |
|-----------|--------|-------------|
| `docs/` (`/home/ben/miru/workbench3/repos/docs`) | read-write | Insert one new version section at the top of `docs/changelog/cli.mdx`. Touch `cspell.json` only if CSpell flags a new word. |
| `cli-private/` (`/home/ben/miru/workbench3/repos/cli-private`) | read-only | Implementation history between tags `v0.12.0` and `v0.12.1`. Read via those tags. Do not modify. |
| `mirurobotics/cli` on GitHub | read-only | Public release tags and notes. There is no local `cli/` checkout. |

This plan lives in `docs/plans/` because the only write is the public CLI changelog in this repo.

Working branch: `docs/cli-v0.12.1-changelog` (already created from latest `main`). Do not create another branch. Base branch for the PR: `main`.


## Purpose / Big Picture

The public CLI changelog at `/changelog/cli` still opens with `# v0.12.0` (*August 27, 2026*). After this change, that page opens with `# v0.12.1` listing only user-facing changes shipped after `v0.12.0`. Use this file's existing categories — Breaking changes, Features, Improvements, Fixes — and omit empty ones. Do not rewrite the existing `# v0.12.0` entry.


## Progress

- [x] Milestone 1: Re-verify sources against the recommended insert.
- [x] Milestone 2: Insert the `# v0.12.1` section into `docs/changelog/cli.mdx` and commit.
- [x] Milestone 3: Run the docs test/lint commands; fix any findings; commit if needed.
- [ ] Milestone 4: Push, open a draft PR, and drive preflight to `CLEAN` (CI green on the pushed head).


## Surprises & Discoveries

- 2026-09-06: Re-verified `v0.12.1` as Latest (`publishedAt` `2026-09-06T18:58:54Z`, not a prerelease) and `v0.12.0` as the previous stable. The `v0.12.0..v0.12.1` commit list still maps user-facing Features to `#214` and `#207`/`#211`/`#212`. No new user-facing command appeared. The recommended insert is unchanged.


## Decision Log

- 2026-09-06: Keep the Plan of Work insert as written (Features only; no schema-selector extra sentence) because Milestone 1 sources still match.


## Outcomes & Retrospective

Summarize at completion.


## Context and Orientation

The docs site is built from MDX under `docs/`. MDX here means Markdown plus JSX components such as `<Dropdown>`. CLI release history is one file, `docs/changelog/cli.mdx`, newest-first. Each version is a `# vX.Y.Z` heading, then an italic date, an optional one-paragraph summary, category sections, and a `---` separator. Navigation is already wired: `docs/docs.json` lists `"changelog/cli"` under the Changelog product. Do not add a new page or a `docs.json` entry. This repo has no `AGENTS.md` or `CLAUDE.md`.

**Latest documented version.** `docs/changelog/cli.mdx` opens at `# v0.12.0` dated *August 27, 2026*. That entry already covers device clone/stage, deployment clone, schema validate, whoami, `MIRU_HOME`, the OAuth login breaking change, the version header, and the unowned-state-directory refusal. Do not repeat those. Do not edit that section.

**Latest shipped version (verified 2026-09-06).** `gh release view v0.12.1 -R mirurobotics/cli` is Latest, not a prerelease, `publishedAt` `2026-09-06T18:58:54Z` (https://github.com/mirurobotics/cli/releases/tag/v0.12.1). Previous stable is `v0.12.0` (`publishedAt` `2026-08-28T03:47:02Z`; the changelog already uses *August 27, 2026*). Date the new entry `*September 6, 2026*` from that UTC calendar day.

**Source of truth, in this order.**

1. `gh release view v0.12.1 -R mirurobotics/cli` — what was packaged. Do not copy those notes verbatim; they include internals.
2. `git -C /home/ben/miru/workbench3/repos/cli-private log v0.12.0..v0.12.1 --oneline --no-merges` — commit messages (13 commits). The `cli-private` working tree may not be on `v0.12.1`; read via tags (`git show v0.12.1:…` / `git log v0.12.0..v0.12.1`). Do not modify that repo.
3. Existing CLI Reference pages on `docs` `main` — command names, flags, and link targets that already shipped via `docs(cli): add validate command references` (#176): `docs/references/cli/config-validate.mdx` → `/references/cli/config-validate`, and `docs/references/cli/deployment-validate.mdx` → `/references/cli/deployment-validate`.

**User-facing** means new or changed commands, flags, login/auth UX, environment variables, user-visible output of already-shipped commands, or breaking behavior. Omit API plumbing, hidden commands, test/build/chore/deps, commands or flags that never shipped in a prior stable release as separate Improvements, and transcript polish or intra-release nits.

**User-facing delta `v0.12.0` → `v0.12.1`.** Features only. No Breaking changes. No Improvements that apply to commands users already had in `v0.12.0`. No Fixes (the blank-line Deployment Valid change is intra-release polish of a command that did not exist in `v0.12.0`).

Include as Features:

- `miru config validate` (`#207`, plus `#211` / `#212` folded in) — check a local config instance against a persisted schema without creating resources. Schema selectors: `--schema-id` or `--release` plus `--config-type` (required together; mutex with `--schema-id`). `--config-type` matches slug or name.
- `miru deployment validate` (`#214`) — check local config files against a release's schemas without creating a deployment. Required `--release`, optional `--root`.

Omit: actions-minor dependency bumps (`#219`, `#206`); `fix(dplcmds): add a blank line before Deployment Valid` (`#218`); internal `dplvldt` / `GetByVersionWithConfigSchemas` / slot-file extract / `cfgschs` / `ConfigInstances.Validate` (`#217`, `#216`, `#215`, `#210`, `#209`, `#208`).

**House style.** Copy the v0.12.0 block in the same file: leave frontmatter and `import { Dropdown } from '/snippets/components/api-dropdown.jsx';` untouched, including the blank line after the import, even if v0.12.1 has no breaking section. Then `#` version heading; italic date; one-paragraph summary; only the non-empty subset of `## Breaking changes`, `## Features`, `## Improvements`, `## Fixes` in that order; hyphen bullets with no trailing period; `<Dropdown>` for breaking-change details; flag names in backticks (the `no-double-dash` linter rejects `--` in prose); em dash (`—`) in prose; trailing `---` before the old `# v0.12.0` section. Feature voice: `Add [`miru …`](/references/cli/…) to …`. Link commands that have CLI Reference pages; do not re-describe the full reference. Sentence-case headings — the `heading-case` linter allows version tags and the tokens `CLI` / `API`.

**Lint and CI.** From `docs/`: `./scripts/lint.sh` runs the Go MDX linter (including `heading-case` and `no-double-dash`), ESLint MDX, CSpell, and `mint openapi-check`. Tests: `pnpm run test:lint`. Validate: `pnpm run validate` (`mint validate`). Preflight: `./scripts/preflight.sh` plus CI. CSpell's allowlist is `cspell.json`; add a word only if lint reports it, in sorted position. **`CLEAN`** means every GitHub Actions check on the pushed branch head is green.


## Plan of Work

Edit only `docs/changelog/cli.mdx`. Insert the new section immediately after the import blank line and before `# v0.12.0`. Do not change frontmatter, the import, or any existing version section.

At implementation time, re-run the verification commands in Milestone 1. If sources still match, use the insert below. If they contradict it, update the bullets to match the sources — do not invent. Optionally mention the two schema selectors (`--schema-id` vs `--release` + `--config-type`, slug-or-name) on the first bullet if it stays one sentence and does not re-describe the reference page. Put flags in backticks.

Use this entry unless Milestone 1 contradicts it. Indentation in the file is column 0 (not nested in another component).

    # v0.12.1

    *September 6, 2026*

    CLI `v0.12.1` adds commands for validating a local config instance and a local deployment against persisted schemas without creating any resources.

    ## Features

    - Add [`miru config validate`](/references/cli/config-validate) to check a local config instance against a persisted schema without creating any resources
    - Add [`miru deployment validate`](/references/cli/deployment-validate) to check local config files against a release's schemas without creating a deployment

    ---

If CSpell flags a word in the new text, add it to `cspell.json` `words` in sorted order and commit it with the changelog. No other files.


## Concrete Steps

### Milestone 1 — Re-verify sources

From any directory:

    gh release view v0.12.1 -R mirurobotics/cli
    gh release view v0.12.0 -R mirurobotics/cli

Expect `v0.12.1` to be Latest and not a prerelease, and `v0.12.0` to remain the previous stable.

From `/home/ben/miru/workbench3/repos/cli-private` (read-only; do not checkout or edit):

    git log v0.12.0..v0.12.1 --oneline --no-merges

Confirm the Features bullets still map to `#214` and `#207`/`#211`/`#212`, and that no new user-facing command appeared.

From `/home/ben/miru/workbench3/repos/docs`:

    git branch --show-current
    git status --short
    test -f docs/references/cli/config-validate.mdx && test -f docs/references/cli/deployment-validate.mdx && echo refs-ok

Expect `docs/cli-v0.12.1-changelog` and a clean tree (this plan file may already be committed or uncommitted; do not create a second branch). Expect `refs-ok`.

Commit step: if sources still match, skip the commit. If they contradict the recommended insert, update this plan's living sections and Plan of Work, then from `/home/ben/miru/workbench3/repos/docs`:

    git add plans/backlog/20260906-cli-v0.12.1-changelog.md
    git commit -m "$(cat <<'EOF'
    docs(plan): correct CLI v0.12.1 changelog sources

    EOF
    )"

One commit for this milestone, and only if the plan itself needed a source correction.


### Milestone 2 — Insert the entry and commit

From `/home/ben/miru/workbench3/repos/docs`, insert the block from Plan of Work. Then:

    grep -n "^# " docs/changelog/cli.mdx

Expect `v0.12.1` first, then `v0.12.0`, `v0.11.0`, … with no duplicate `v0.12.1`.

    git diff --stat

Expect only `docs/changelog/cli.mdx` (insertions only) unless CSpell required an edit.

    git add docs/changelog/cli.mdx
    git commit -m "$(cat <<'EOF'
    docs(changelog): add CLI v0.12.1 release notes

    EOF
    )"

If `cspell.json` changed, stage it in the same commit. One commit for this milestone.


### Milestone 3 — Test and lint

From `/home/ben/miru/workbench3/repos/docs` (run `pnpm install` first if `node_modules/` is missing):

    pnpm run test:lint
    ./scripts/lint.sh
    pnpm run validate

Expect: lint smoke tests pass; `./scripts/lint.sh` prints that documentation lint checks passed and CSpell reports 0 issues; `pnpm run validate` prints that build validation passed. These are the repo's tests for MDX prose — there is no changelog unit test. If a check fails, fix the entry (or `cspell.json`) and make a **new** commit, then re-run the three commands.

Commit step: if all three commands exit 0 with no file changes, skip the commit. If a fix was required, from `/home/ben/miru/workbench3/repos/docs`:

    git add docs/changelog/cli.mdx
    git commit -m "$(cat <<'EOF'
    docs(changelog): fix CLI v0.12.1 lint findings

    EOF
    )"

Stage `cspell.json` in that same commit if it changed. One commit for this milestone, and only if a fix was needed.


### Milestone 4 — Publish and preflight

From `/home/ben/miru/workbench3/repos/docs`:

    git fetch origin main
    git rebase origin/main
    git push -u origin HEAD

If the rebase rewrote commits that were already on the remote, `git push --force-with-lease` is required; that is the expected follow-up, not a destructive reset. Then, if no PR exists:

    gh pr create --draft --base main --title "docs(changelog): add CLI v0.12.1 release notes" --body "$(cat <<'EOF'
    ## Summary
    - Add the `# v0.12.1` section to `docs/changelog/cli.mdx` for user-facing CLI changes since `v0.12.0`.

    ## Test plan
    - [ ] `pnpm run test:lint`, `./scripts/lint.sh`, and `pnpm run validate` exit 0
    - [ ] `/changelog/cli` opens with `# v0.12.1` and the v0.12.0 section is unchanged
    - [ ] Preflight reports CLEAN (CI green on this head)

    EOF
    )"

Watch CI on the current head:

    git rev-parse HEAD
    gh pr checks --watch

If any check is red, fetch the failed logs (`gh run view <id> --log-failed`), fix, **new** commit, push once, and watch again. Repeat until every check is green for that head SHA.

Commit step: if CI is green on the first pushed head, skip the commit. If a CI fix was required, from `/home/ben/miru/workbench3/repos/docs` commit only the fix (one commit), push, and re-watch.

Preflight must report `CLEAN` (CI green on the pushed branch head) before the PR leaves draft and before this task is reported complete. A green local `./scripts/preflight.sh` is useful but not sufficient. A green run on an older commit is not sufficient.


## Validation and Acceptance

1. `docs/changelog/cli.mdx` starts its body (after frontmatter + import) with `# v0.12.1`, `*September 6, 2026*`, a one-paragraph summary, `## Features` only (no empty Breaking / Improvements / Fixes headings), then `---`, then the untouched `# v0.12.0` section.
2. The Features bullets name `miru config validate` and `miru deployment validate` only, and both link targets exist under `docs/references/cli/`.
3. No bullet repeats a v0.12.0 feature, documents API-client internals, or treats the blank-line Deployment Valid change as a Fix.
4. `grep -n "^# " docs/changelog/cli.mdx` lists `v0.12.1` then `v0.12.0` then older versions, each once.
5. `git diff main -- docs/changelog/cli.mdx` is a pure insertion at the top of the body (plus a `cspell.json` words-only edit if lint required it). No other path in the diff.
6. From `/home/ben/miru/workbench3/repos/docs`, `pnpm run test:lint`, `./scripts/lint.sh`, and `pnpm run validate` all exit 0.
7. **Preflight reports `CLEAN`.** `gh pr checks` shows every GitHub Actions check passing for the current pushed head SHA. Local `pnpm run test:lint` / `./scripts/lint.sh` / `pnpm run validate` (criterion 6) plus this CI-green head must both hold before the PR leaves draft and before this task is reported complete.


## Idempotence and Recovery

Inserting the section is a text edit. Re-running Milestone 2 on a tree that already has `# v0.12.1` at the top is a no-op if the content matches; if it drifted, replace only that section. `git revert` of the Milestone 2 commit restores the pre-change file. Lint commands are read-only aside from building `tools/lint/lint` (gitignored). If CI fails after a push, fix the cause and add a new commit — do not amend a pushed commit. The only force-push this plan allows is `--force-with-lease` after rebasing onto `origin/main`.
