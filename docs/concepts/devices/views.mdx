---
title: "Device views"
---

import { PlatformApiBadge, PlatformUnsupportedBadge } from '/snippets/components/platform-api-link.jsx';
import { ViewerBadge } from '/snippets/components/role-badges.jsx';

The [Devices page](https://app.mirurobotics.com/devices) lists every device in your
workspace. Use search, filters, and display options to find the devices you need, and
save them as views to come back to later.

## Search devices  <ViewerBadge />

<PlatformApiBadge endpoint="devices/list" />

Click the search icon above the list, type, and press **Enter**. Search matches device
names by default; switch the field to **Description** to search descriptions instead.

<Frame>
  ![Device Search](https://assets.mirurobotics.com/docs/v04/images/devices/views/search.png)
</Frame>

## Filter devices  <ViewerBadge />

<PlatformApiBadge endpoint="devices/list" />

Click the filter icon and filter by status, release, group, or agent version. You can
also find devices with no release, group, or agent version.

<Frame>
  ![Device Filter Menu](https://assets.mirurobotics.com/docs/v04/images/devices/views/filter-menu.png)
</Frame>

Selecting multiple values in the same filter widens the list: online and offline shows
devices that are online *or* offline. Adding another filter narrows it: online with
release `v2.0.0` shows devices that are online *and* on `v2.0.0`.

Active searches and filters appear as chips above the list. Click a chip's × to remove
it, or **Clear** to remove them all. Filters are saved in the page's URL, so you can
bookmark or share a filtered list.

## Display options  <ViewerBadge />

Click the display icon to group the list by status, release, group, or agent version,
sort it by name or status, switch to compact rows, or show and hide columns.

<Frame>
  ![Device Display Options](https://assets.mirurobotics.com/docs/v04/images/devices/views/display-menu.png)
</Frame>

## Saved views  <ViewerBadge />

<PlatformUnsupportedBadge />

A saved view keeps a search, filters, grouping, sort order, and visible columns so you can
return to them in one click. Saved views appear as tabs above the list, after **Active**
and **Archived**. Each group on the [Groups page](https://app.mirurobotics.com/groups) has
its own saved views, separate from these.

<Frame>
  ![Saved Device Views](https://assets.mirurobotics.com/docs/v04/images/devices/views/saved-views.png)
</Frame>

### Save a view

Search and filter the list, then click **Save** next to the filter chips. Name the view
and click **Save**. A view needs at least one search or filter.

Views are shared with the workspace by default. Turn off **Share with workspace** to keep
a view to yourself; private views show a lock icon. Anyone who can see a shared view can
edit or delete it.

### Update a view

When you change the view you're on, a dot appears on its tab. Click **Save** to keep the
changes, or **Discard** to undo them.

### Edit, duplicate, or delete a view

Right-click a view's tab to edit its name, description, or sharing, duplicate it, or
delete it. Deleting a shared view removes it for everyone.
