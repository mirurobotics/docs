<Tabs>
  <Tab title="Schema ID">
    ```bash
    miru config validate {file} --schema-id {config-schema-id}
    ```
  </Tab>

  <Tab title="Release and config type">
    ```bash
    miru config validate {file} --release {release-version} --config-type {config-type}
    ```
  </Tab>
</Tabs>
