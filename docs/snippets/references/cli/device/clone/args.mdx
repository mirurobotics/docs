<ParamField path="device-name" type="string" required>
  Device name as the dashboard shows it. Names are unique within a workspace.

  The device must have a current deployment. To clone a different deployment, use [`miru deployment clone`](/references/cli/deployment-clone).

  Examples: `robot-42`
</ParamField>
