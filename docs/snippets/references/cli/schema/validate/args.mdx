<ParamField path="file" type="string" required>
  One or more schema files.

  The files must form exactly one schema. Several files are one schema when they belong to one [CUE package](/cfg-mgmt/concepts/schemas/languages/cue). Schemas must be annotated the same way they are for a [release](/cfg-mgmt/concepts/schemas/annotations/overview).

  Examples: `./path/to/schema.json`, `first.cue second.cue`
</ParamField>
