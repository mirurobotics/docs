`login` authenticates a user in the browser and does not use an API key.

For non-interactive use, authenticate with `MIRU_API_KEY` instead — see [Authentication](/developers/cli/authentication#api-key). Each command's **API key scopes** section lists the scopes that key needs.
