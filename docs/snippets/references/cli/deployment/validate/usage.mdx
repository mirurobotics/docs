<Tabs>
  <Tab title="Current directory">
    ```bash
    miru deployment validate --release {release-version}
    ```
  </Tab>

  <Tab title="Custom root">
    ```bash
    miru deployment validate --release {release-version} --root {root-dir}
    ```
  </Tab>

  <Tab title="Device paths">
    ```bash
    miru deployment validate --release {release-version} --root /
    ```
  </Tab>
</Tabs>
