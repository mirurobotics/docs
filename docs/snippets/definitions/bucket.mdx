A **bucket** is cloud storage you own and register with Miru as a destination for uploads. The [Miru Agent](/developers/agent/overview) uploads data straight into it — your data never passes through Miru's infrastructure, only the request to mint an upload URL does.
