An **upload rule** is a standing instruction: whenever files matching a source pattern appear on a device, upload them to a [bucket](/data-uploads/buckets/overview). Each rule belongs to an [upload collection](/data-uploads/upload-collections), is immutable once created, and is shipped to devices as part of a [release](/fundamentals/releases/overview).
