An **upload rule** is a standing instruction: whenever data matching a source pattern appears on a device, upload it to a [bucket](/data-uploads/buckets/overview). Each rule belongs to an upload type, is immutable once created, and is shipped to devices as part of a [release](/learn/releases/overview).
