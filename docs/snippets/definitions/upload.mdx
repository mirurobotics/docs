An **upload** is the ledger entry for a single file the [Miru Agent](/developers/agent/overview) has collected from a device and written to your [bucket](/data-uploads/buckets/overview). Each upload links back to the device, rule, collection, release, and deployment that produced it.
