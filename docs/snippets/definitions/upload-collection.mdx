An **upload collection** is a named grouping of [file rules](/data-uploads/primitives/file-rules) and their [uploads](/data-uploads/primitives/uploads). 

Every upload belongs to exactly one collection, as does every file rule that uploads. Collections give the data your fleet records a stable, human-readable identity (`Robot Logs`, `Crash Reports`) that survives across the rules and uploads that come and go beneath it.
