A dialog will appear prompting you to install the `miru-agent`. If the device already has a compatible `miru-agent` package installed, you can skip this step.

<Frame>
  ![Install Miru Agent Dialog](/images/install-dialog.png)
</Frame>

<Note>
  For more information about installing the `miru-agent` package, visit the [agent installation](/developers/agent/install) documentation.
</Note>
