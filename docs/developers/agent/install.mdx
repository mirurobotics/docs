---
title: 'Install'
---

import Install from '/snippets/agent/install/install.mdx';
import SupportedPlatforms from '/snippets/agent/supported-platforms.mdx';
import Uninstall from '/snippets/agent/install/uninstall.mdx';
import Verify from '/snippets/agent/install/verify.mdx';
import GithubDownload from '/snippets/agent/install/github-download.mdx';

This page details the process of installing the Miru Agent Debian package. 

It does not cover provisioning devices for your workspace. Visit the [device provisioning](/docs/learn/devices/provision/dashboard) documentation to learn how to create and register devices with your workspace.

## Supported platforms

<SupportedPlatforms />

## Install

<Install />

## Upgrade

As with the installation process, you can upgrade the `miru-agent` package via `apt` or manually by downloading and installing the new package. 

<Warning>
    If you're upgrading to a `miru-agent` version that precedes `v0.9.0`, you MUST [reactivate](/docs/learn/devices/provision/reactivate) the device after upgrading the `miru-agent` package.
</Warning>

Since both methods use Debian packages, manual installations can be upgraded via `apt` and vice versa.

<Tabs>
    <Tab title="APT">
        Upgrade the existing `miru-agent` via `apt`.

        ```bash
        sudo apt-get update
        sudo apt-get install miru-agent
        ```

        <Tip>
            Upgrade to a specific version with `sudo apt-get install miru-agent=<version>` (e.g. `0.7.0`, `0.8.0`, etc.).
        </Tip>
    </Tab>

    <Tab title="Manual">
        Manual installations do not have a native upgrade mechanism. Simply repeat the [installation steps](#install) to upgrade to the latest version.
    </Tab>
</Tabs>

## Uninstall

<Uninstall />

## Poor connectivity

Some environments may have a poor internet connection. Whether located in a remote area or using a cellular connection, downloading the Miru Agent over a network may be impractical in some environments.

In such scenarios, we recommend downloading the Miru Agent to a computer with a stable connection. Then, transfer the package to your target machine and install it.

While this method circumvents the need to _download_ the Miru Agent over a network, _activating_ the agent still requires some level of connectivity to register the device with the Miru servers.

<Steps>
    <Step title="Download the agent">
        Follow the [manual installation steps](#manual) to download the Miru Agent to a computer with a stable connection.
    </Step>
    <Step title="Transfer the agent">
        Once you've downloaded the appropriate Debian package, transfer it to your target machine. This can be done via a variety of methods, such as a USB drive.
    </Step>
    <Step title="Install the agent">
        Once you've transferred the package to your target machine, install the `miru-agent` package by running:

        ```bash
        sudo dpkg -i <...>.deb
        ```
    </Step>
</Steps>
